pipeline {
  agent any {
    stages {
      stage('Build') {
        sh 'echo Build'
      }
      stage('Test') {
          sh 'echo Test'
      }
      stage('Infra Creation') {
          sh 'echo Infra Creation'
      }
      stage('Deploy') {
          sh 'echo Deploy'
      }
    }
  }
}
